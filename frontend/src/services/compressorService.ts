// src/services/compressorService.ts

import { CompressorStatus } from "@/types/compressor"

// export const fetchCompressorStatuses = async (): Promise<CompressorStatus[]> => {
//   // Replace with real API call
//   return ['on', 'warning', 'off'];
// };
